#include<iostream>
using namespace std;
int main()
{
    int queue[10];
    int element;
    int ch;
    int size;
    cout<<"enter the choice n for finish"<<endl;
    
    cin>>ch;
    
    while(ch!=2)
    {
       // cout<<"enter the choice n for finish";
       // cin>>ch;
        for(int i=0;i<size;i++)
    {
        cout<<"\nenter the value of index "<<i<<" = " ;
        cin>>queue[i];
    }
    }
}